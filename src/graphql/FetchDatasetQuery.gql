query FetchDatasetQuery($allFactSheetsFilter: FilterInput) {
  taggedFactSheets: allFactSheets(filter: $allFactSheetsFilter) {
    totalCount
    edges {
      node {
        id
        name
        displayName
        type
        tags { id name color tagGroup{ id name } }
      }
    }
  }
}
